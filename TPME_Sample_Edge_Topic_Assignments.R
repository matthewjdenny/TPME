#script to define SAMPLE_DOCUMENT_EDGE_ASSIGNMENTS

SAMPLE_DOCUMENT_EDGE_ASSIGNMENTS <- function(edge_topic_assignments,latent_space_positions,latent_space_intercepts,latent_dimensions,document_author,document_edge_matrix,number_of_tokens_in_document){
    
    new_edge_topic_assignments <- rep(0,length(edge_topic_assignments))
    
    
    
    return(new_edge_topic_assignments)
}