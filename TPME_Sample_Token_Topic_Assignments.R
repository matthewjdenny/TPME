#this script defines the SAMPLE_TOKEN_TOPIC_ASSIGNMENTS function


SAMPLE_TOKEN_TOPIC_ASSIGNMENTS <- function(document_token_topic_assignments,number_of_topics,number_of_authors,alpha_base_measure_vector, beta,number_of_words,edge_topic_assignments,latent_space_positions,latent_space_intercepts,latent_dimensions,document_author,document_edge_matrix){
    
    new_token_topic_assignments <- rep(0,length(document_token_topic_assignments))
    
    
    
    
    
    return(new_token_topic_assignments)
}